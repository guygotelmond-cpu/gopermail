FROM golang:1.26-alpine AS build
ENV GOTOOLCHAIN=auto
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /out/smtpd ./cmd/smtpd

FROM alpine:3.20
COPY --from=build /out/smtpd /usr/local/bin/smtpd
EXPOSE 1025
ENTRYPOINT ["/usr/local/bin/smtpd"]
